#!/bin/bash

# Installs util requirements for linux
# using python venv or anaconda is recommended; but this will very likely not break anything
sudo apt install python3-pip
pip install -r util_requirements.txt